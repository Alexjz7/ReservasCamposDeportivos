@model IEnumerable<WebCamposDeportivos_V1._2.Models.Deportes>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.estado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_deporte">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_deporte">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_deporte">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
