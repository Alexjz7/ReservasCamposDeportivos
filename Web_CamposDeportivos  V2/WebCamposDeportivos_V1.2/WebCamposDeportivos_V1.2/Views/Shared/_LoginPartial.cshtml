﻿@if (Context.User.Identity.IsAuthenticated == true)
{
    <span>
        @Context.User.Identity.Name
    </span>
}
@* else
{
    <a class="nav-link text-dark" asp-controller="Access" asp-action="Login">Iniciar sesión</a>
} *@
